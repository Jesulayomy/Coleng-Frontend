import Information from '@/components/Information';
import React from 'react';


export const metadata = {
  title: 'Info',
};

const page = () => {
  return (
    <Information />
  );
};

export default page;

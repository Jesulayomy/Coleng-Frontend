import Resources from '@/components/Resources';
import React from 'react';


export const metadata = {
  title: 'Recources',
};

const page = () => {
  return (
    <Resources />
  );
};

export default page;
